#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export DEMISTO_SDK_IGNORE_CONTENT_WARNING="${DEMISTO_SDK_IGNORE_CONTENT_WARNING:-true}"

exec "$ROOT_DIR/.venv/bin/demisto-sdk" "$@"
